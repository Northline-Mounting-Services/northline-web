---
const facts = [
  { label: "Warranty", fact: "5-year installation warranty" },
  { label: "Insured", fact: "$1,000,000 liability coverage" },
  { label: "After the job", fact: "Packaging removed \u2014 free" }
];
---

<section class="nl-trust" aria-label="Warranty, insurance and cleanup">
  {facts.map((item) => (
    <div class="nl-trust__cell">
      <p class="nl-kicker">{item.label}</p>
      <p class="nl-trust__fact">{item.fact}</p>
    </div>
  ))}
</section>
