<section
  class="nl-package"
  aria-labelledby="nl-fireplace-package-heading"
  data-fireplace-package
>
  <h2 class="nl-kicker" id="nl-fireplace-package-heading">Package</h2>

  <div class="nl-tabs" role="tablist" aria-label="Fireplace installation">
    <button
      class="nl-tab is-active"
      type="button"
      role="tab"
      aria-selected="true"
      data-fp-mode="standard"
    >
      Standard
    </button>

    <button
      class="nl-tab"
      type="button"
      role="tab"
      aria-selected="false"
      data-fp-mode="package"
    >
      Package
    </button>
  </div>

  <div class="nl-panel" data-fp-panel="standard">
    <p class="nl-price">
      <span class="nl-price__label">from</span>
      <span class="nl-price__value" data-fp-standard-price>&mdash;</span>
    </p>

    <p class="nl-package__descriptor">
      Above-fireplace mounting on drywall.
    </p>

    <button class="nl-action" type="button" data-nl-open>
      <span>Calculate price &amp; book</span>
      <span aria-hidden="true">&rarr;</span>
    </button>
  </div>

  <div class="nl-panel" data-fp-panel="package" hidden>
    <p class="nl-price">
      <span class="nl-price__value" data-fp-package-price>&mdash;</span>
    </p>

    <p class="nl-package__descriptor" data-fp-package-description>
      Package pricing is loading.
    </p>

    <div class="nl-qty">
      <p class="nl-kicker">Surface</p>

      <div class="nl-qty__row">
        <button
          class="nl-chip is-active"
          type="button"
          aria-pressed="true"
          data-fp-package-id="NL-PKG-FIRE"
        >
          Brick / Stone
        </button>

        <button
          class="nl-chip"
          type="button"
          aria-pressed="false"
          data-fp-package-id="NL-PKG-FRAME"
        >
          Tile / Granite
        </button>
      </div>
    </div>

    <button class="nl-action" type="button" data-fp-book disabled>
      <span>Book installation</span>
      <span aria-hidden="true">&rarr;</span>
    </button>
  </div>
</section>

<script>
  type PublicPackage = {
    packageId: string;
    name: string;
    description: string;
    priceCents: number;
  };

  type BookingWindow = Window & {
    openPackageBooking?: (pkg: {
      packageId: string;
      category: string;
      priceLabel: string;
      description: string;
    }) => void;
  };

  const root =
    document.querySelector<HTMLElement>("[data-fireplace-package]");

  if (root) {
    const tabs = Array.from(
      root.querySelectorAll<HTMLButtonElement>("[data-fp-mode]")
    );

    const panels = Array.from(
      root.querySelectorAll<HTMLElement>("[data-fp-panel]")
    );

    const chips = Array.from(
      root.querySelectorAll<HTMLButtonElement>("[data-fp-package-id]")
    );

    const standardPrice =
      root.querySelector<HTMLElement>("[data-fp-standard-price]");

    const packagePrice =
      root.querySelector<HTMLElement>("[data-fp-package-price]");

    const packageDescription =
      root.querySelector<HTMLElement>("[data-fp-package-description]");

    const book =
      root.querySelector<HTMLButtonElement>("[data-fp-book]");

    const packages = new Map<string, PublicPackage>();

    let selectedPackage: PublicPackage | null = null;

    const money = (cents: number) =>
      "$" +
      (cents % 100 === 0
        ? String(cents / 100)
        : (cents / 100).toFixed(2));

    const setMode = (mode: string) => {
      tabs.forEach((tab) => {
        const active = tab.dataset.fpMode === mode;

        tab.classList.toggle("is-active", active);
        tab.setAttribute("aria-selected", String(active));
      });

      panels.forEach((panel) => {
        panel.hidden = panel.dataset.fpPanel !== mode;
      });
    };

    const selectPackage = (packageId: string) => {
      chips.forEach((chip) => {
        const active =
          chip.dataset.fpPackageId === packageId;

        chip.classList.toggle("is-active", active);
        chip.setAttribute("aria-pressed", String(active));
      });

      const pkg = packages.get(packageId);

      if (!pkg) {
        selectedPackage = null;

        if (packagePrice) packagePrice.textContent = "—";

        if (packageDescription) {
          packageDescription.textContent =
            "Package pricing is temporarily unavailable.";
        }

        if (book) book.disabled = true;

        return;
      }

      selectedPackage = pkg;

      if (packagePrice) {
        packagePrice.textContent = money(pkg.priceCents);
      }

      if (packageDescription) {
        packageDescription.textContent = pkg.description;
      }

      if (book) book.disabled = false;
    };

    tabs.forEach((tab) => {
      tab.addEventListener("click", () => {
        setMode(tab.dataset.fpMode || "standard");
      });
    });

    root
      .querySelector('[role="tablist"]')
      ?.addEventListener("keydown", (event) => {
        if (!(event instanceof KeyboardEvent)) return;

        if (
          event.key !== "ArrowRight" &&
          event.key !== "ArrowLeft"
        ) {
          return;
        }

        event.preventDefault();

        const current =
          tabs.findIndex(
            (tab) =>
              tab.getAttribute("aria-selected") === "true"
          );

        const offset =
          event.key === "ArrowRight" ? 1 : tabs.length - 1;

        const next =
          tabs[(current + offset) % tabs.length];

        setMode(next.dataset.fpMode || "standard");
        next.focus();
      });

    chips.forEach((chip) => {
      chip.addEventListener("click", () => {
        const packageId = chip.dataset.fpPackageId;

        if (packageId) selectPackage(packageId);
      });
    });

    book?.addEventListener("click", () => {
      if (!selectedPackage) return;

      const open =
        (window as BookingWindow).openPackageBooking;

      if (typeof open !== "function") return;

      open({
        packageId: selectedPackage.packageId,
        category: selectedPackage.name,
        priceLabel: money(selectedPackage.priceCents),
        description: selectedPackage.description
      });
    });

    fetch("/api/calculator-pricing", {
      headers: { accept: "application/json" }
    })
      .then((response) => {
        if (!response.ok) throw new Error("pricing_unavailable");
        return response.json();
      })
      .then((pricing) => {
        const base =
          pricing.items?.["standard_tv.tv_size.up_to_50"];

        const fireplace =
          pricing.items?.["standard_tv.location.fireplace"];

        if (
          typeof base !== "number" ||
          typeof fireplace !== "number"
        ) {
          throw new Error("standard_price_unavailable");
        }

        if (standardPrice) {
          standardPrice.textContent =
            `$${Math.round(base + fireplace)}`;
        }
      })
      .catch(() => {});

    fetch("/api/packages", {
      headers: { accept: "application/json" }
    })
      .then((response) => {
        if (!response.ok) throw new Error("packages_unavailable");
        return response.json();
      })
      .then((data) => {
        if (!Array.isArray(data.packages)) {
          throw new Error("invalid_packages");
        }

        data.packages.forEach((pkg: PublicPackage) => {
          if (
            pkg.packageId === "NL-PKG-FIRE" ||
            pkg.packageId === "NL-PKG-FRAME"
          ) {
            packages.set(pkg.packageId, pkg);
          }
        });

        selectPackage("NL-PKG-FIRE");
      })
      .catch(() => {
        selectPackage("NL-PKG-FIRE");
      });

    fetch("/api/home-state", {
      headers: { accept: "application/json" }
    })
      .then((response) => {
        if (!response.ok) throw new Error("home_state_unavailable");
        return response.json();
      })
      .then((data) => {
        if (!data?.ok) return;

        const today =
          document.querySelector<HTMLElement>(
            '[data-avail="today"]'
          );

        const next =
          document.querySelector<HTMLElement>(
            '[data-avail="next"]'
          );

        const todayLabel =
          data.availability?.today?.label;

        const nextLabel =
          data.availability?.next?.label;

        if (
          today &&
          typeof todayLabel === "string" &&
          todayLabel
        ) {
          today.textContent = todayLabel;
        }

        if (
          next &&
          typeof nextLabel === "string" &&
          nextLabel
        ) {
          next.textContent = nextLabel;
        }
      })
      .catch(() => {});

    setMode("standard");
  }
</script>
